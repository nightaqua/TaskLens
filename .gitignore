# --- Dependencies ---
node_modules/
# package-lock.json now needed for workflow automation

# --- Build Outputs ---
# Main plugin code generated by esbuild
main.js
# CSS/SCSS
/styles.css 
# Manifest is source, do not ignore
# manifest.json

# --- Obsidian specific ---
# If you are developing inside a vault, ignore the .obsidian config folder
.obsidian/
# Ignore the entire test vault if present
test-vault/

# --- System Files ---
*.DS_Store
Thumbs.db
data.json
*.log


# --- IDEs ---
.idea/
.vscode/
*.iml
.idea
*.swp
*.swo

# Private maintainer notes and release drafts (local only — never commit)
docs/00-ForMe.md
docs/announcement-*.md

# --- Testing ---
coverage/
# ESLint report written by the code-quality agent (repo-local, never commit)
eslint-report.json

# --- Temporary ---
tmp/
temp/

# AI agent working files
.jules/
.Jules/
.iloom/
# Agent coordination dir — local-only operational files, never commit
.agents/
# Claude Code project config (contains symlink to agent definitions)
.claude/

# TypeScript incremental build info
tsconfig.tsbuildinfo
*.tsbuildinfo

# Source maps (if esbuild ever emits them)
*.js.map

# Test output
junit.xml
test-results/

# Environment files
.env
.env.local